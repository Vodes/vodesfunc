ass>=0.5.2
py7zr>=0.20.0
wget>=3.2
vstools>=1.6.2
vskernels>=1.2.0
vsrgtools>=1.3.0
vsscale>=1.0.1
fontTools>=4.37.1
ebmlite>=3.3.0
pyparsebluray>=0.1.4
Vapoursynth>=59
# vardefunc>=0.7.2
# Somewhat optional, only needed for vodes_rescale and only if using a mode > 0

# vsdenoise>=69
# Haha le funny
# go publish it already