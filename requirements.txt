ass>=0.5.2
py7zr>=0.20.0
wget>=3.2
vsutil>=0.8.0
vskernels>=1.2.0
vsscale>=1.0.1
fontTools>=4.37.1
numpy>=1.23.0
ebmlite>=3.3.0
pyparsebluray>=0.1.4
pytimeconv>=0.0.2
Vapoursynth>=59
# vardefunc>=0.7.2
# Somewhat optional, only needed for vodes_rescale and only if using a mode > 0

# vsdenoise>=69
# Haha le funny
# go publish it already